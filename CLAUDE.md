## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `stratjan/nodule_management`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
